let overTextImg = document.querySelectorAll('.over-text');
    for (let i = 0; i < overTextImg.length; i++) {
        overTextImg[i].classList.add("hide");
}


for (let i = 0; i < overTextImg.length; i++) {
    overTextImg[i].onmouseover = function () {
        overTextImg[i].classList.add("show");
    }
}

for (let i = 0; i < overTextImg.length; i++) {
    overTextImg[i].onmouseover = function () {
        overTextImg[i].classList.add("show");
    }
}